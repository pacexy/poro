/*! THIS FILE IS AUTO-GENERATED */

const axios = require('./axios')

{% for apiName, api in apis %}
  const {{ apiName|PascalCase }} = require('./apis/{{ apiName }}')
{% endfor %}

const riot = {
  setAuth(auth) {
    axios.defaults.headers.common['X-Riot-Token'] = auth
  },
  {% for apiName, api in apis %}
    {{ apiName|PascalCase }},
  {% endfor %}
}

module.exports = riot
