module.exports = [
	{ field: "AllName", type: "String", desc: "Case & special character insensitive list of redirects to team page, including the page itself" },
	{ field: "OtherName", type: "String", desc: "Not in use. Null for the page the content belongs to; holds a value of the redirect for each redirect" },
	{ field: "UniqueLine", type: "String", desc: "Not in use. A unique field for each line, if we wanted case-sensitive field values, which we don't" },
]
